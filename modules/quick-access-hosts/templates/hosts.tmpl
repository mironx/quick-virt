# Generated by quick-access-hosts — do not edit manually
# Edits will be overwritten on next `terraform apply`.

%{ for vm in vms ~}
# ${vm.name}
%{ for entry in vm.entries ~}
${format("%-16s", entry.ip)} ${entry.host}
%{ endfor ~}

%{ endfor ~}
