# Generated by quick-access-ansible — do not edit manually
# Edits will be overwritten on next `terraform apply`.

# ---- Host declarations -------------------------------------------------------
%{ for vm in vms ~}
${vm.name} ansible_host=${vm.primary_ip}
%{ endfor ~}

# ---- Groups ------------------------------------------------------------------
%{ for group_name, vm_names in groups ~}
[${group_name}]
%{ for vm_name in vm_names ~}
${vm_name}
%{ endfor ~}

%{ endfor ~}
# ---- Global vars -------------------------------------------------------------
[all:vars]
ansible_user=${ssh.user}
ansible_ssh_private_key_file=${ssh.identity_file}
%{ if ssh.password != null ~}
ansible_password=${ssh.password}
%{ endif ~}
%{ if ssh.become_password != null ~}
ansible_become_password=${ssh.become_password}
%{ endif ~}